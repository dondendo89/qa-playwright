{
  "name": "qa-playwright",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "db:migrate": "pnpm -F prisma migrate:deploy",
    "db:seed": "pnpm -F prisma db:seed"
  },
  "devDependencies": {
    "turbo": "^1.13.4"
  },
  "packageManager": "pnpm@8.6.0"
}